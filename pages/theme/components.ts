import { ButtonConfig as Button } from '../core/common/components/Button';
import { SelectConfig as Select } from '../core/common/components/Select';

export const components = {
  Button,
  Select,
};
